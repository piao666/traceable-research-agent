# Demo Research Note

This local document is used by the `file_reader` smoke path.

The platform keeps a trace for every read, records the normalized path, limits
the number of characters returned, and turns failures into visible failed
traces. A report is generated from observations rather than hidden logs.

The demo also exercises the read-only SQL policy and Markdown report writer.
