# Traceable Research Agent Task Log

## Project Paths

- Independent repo: `E:\BOSS\traceable-research-agent`
- GitHub repo: `https://github.com/piao666/traceable-research-agent`
- Project planning document: `E:\BOSS\Traceable_Research_Agent_独立项目规划与GPT_Researcher必读结果.docx`
- GPT Researcher read-only reference: `E:\BOSS\_reference_gpt_researcher`

## Original GPT Researcher Reading Notes

Status: initial reading pass completed on 2026-06-15.

Files actually read:

- `README.md`
- `main.py`
- `backend/server/app.py`
- `gpt_researcher/agent.py`
- `gpt_researcher/skills/researcher.py`
- `gpt_researcher/skills/writer.py`
- `gpt_researcher/document/document.py`
- `gpt_researcher/mcp/README.md`
- `gpt_researcher/mcp/client.py`
- `gpt_researcher/mcp/tool_selector.py`
- `gpt_researcher/mcp/research.py`
- `gpt_researcher/mcp/streaming.py`
- `docker-compose.yml`
- `Dockerfile`

Reading conclusions:

- GPT Researcher is a web/local deep research agent. It focuses on turning a
  query into research questions, gathering sources, aggregating context, and
  producing cited reports.
- The README architecture uses planner + execution agents + publisher/report
  writer. This is useful for our planner/executor/reporter mental model.
- `main.py` is thin: load `.env`, import `backend.server.app`, and start uvicorn
  on `0.0.0.0:8000`.
- `backend/server/app.py` uses FastAPI, CORS, static files, report endpoints,
  `/report/`, `/api/reports`, `/api/chat`, `/files/`, `/upload/`,
  file deletion, and WebSocket communication. It uses a lightweight
  `reports.json` style report store rather than a task/trace database.
- `gpt_researcher/agent.py` defines `GPTResearcher`, initializes config,
  retrievers, memory, ResearchConductor, ReportGenerator, ContextManager,
  BrowserManager, SourceCurator, optional image generation, and MCP strategy.
  `conduct_research()` handles agent selection and context gathering.
  `write_report()` delegates final report generation. `research_costs` and
  `step_costs` track LLM cost, but not structured per-tool execution traces.
- `gpt_researcher/skills/researcher.py` branches by source: web, local,
  hybrid, Azure, LangChain documents, and vector store. It plans sub-queries,
  searches/scrapes, uses vector store when configured, and integrates MCP
  retrievers with fast/deep/disabled style behavior.
- `gpt_researcher/skills/writer.py` builds report parameters from query, role,
  report type/source, tone, websocket, config, and headers. It generates final
  report, introduction, conclusion, subtopics, and draft section titles.
- `gpt_researcher/document/document.py` walks local paths or file lists and
  dispatches loaders by extension. Supported formats include PDF, txt, doc,
  docx, pptx, csv, xls/xlsx, md, html, and htm.
- `gpt_researcher/mcp/*` manages MCP server config conversion, client
  lifecycle, LLM-based tool selection with fallback pattern matching, tool
  execution, result normalization, WebSocket streaming, and security guidance.
- Docker runs backend on 8000 and Next.js frontend on 3000, mounting local
  `my-docs`, `outputs`, and `logs`, with API keys supplied through environment
  variables.

What can be borrowed:

- Planner/executor/reporter conceptual split.
- Multi-source research pattern: web/local/hybrid/vector/MCP.
- Report generation from task, role, context, and evidence rather than raw
  output concatenation.
- Local document format support ideas.
- MCP config concepts, client lifecycle, read-only token handling, and fallback
  tool selection ideas.
- Docker pattern of mounting input/output/log directories and injecting secrets
  through environment variables.

What must be redesigned:

- Build an independent FastAPI API around runs, statuses, tool registry, traces,
  and reports instead of reusing `/report/` and `reports.json`.
- Persist every tool call as a `tool_traces` row; GPT Researcher logs and cost
  counters are not enough.
- Represent file, SQL, RAG, GitHub/MCP, and report writing as registered tools
  with structured input/output schemas.
- Add explicit safety gates for file paths, read-only SQL, high-risk tools, and
  later human confirmation.
- Start with deterministic JSON plans before autonomous LLM tool selection.
- Keep MCP/GitHub as a later read-only enhancement so Phase 1-3 are not blocked.

## Project Planning Document Reading Notes

Status: read on 2026-06-15 using Python `python-docx` from the bundled Codex
document runtime. The file was located with a `Traceable_Research_Agent_*.docx`
glob to avoid Chinese filename encoding issues.

Project positioning:

- This repository is the second independent Agent engineering project.
- It should not be implemented inside another Agent+RAG backend repository.
- It references GPT Researcher only for design ideas.
- The core value is task execution, tool safety, trace persistence, backend
  APIs, and evaluation.

Final target:

- Accept complex research tasks and create a `run_id`.
- Maintain pending/running/completed/failed/waiting_human state.
- Plan steps, execute registered tools, collect evidence, and generate
  Markdown reports.
- Expose task creation, status query, trace query, report query, and tool-list
  APIs.
- Provide smoke demos, eval cases, README, trace examples, bad cases, and
  interview-ready explanations.

API design:

- `GET /health`
- `POST /api/tasks`
- `GET /api/tasks/{run_id}`
- `GET /api/tasks/{run_id}/trace`
- `GET /api/reports/{run_id}`
- `GET /api/tools`
- `POST /api/tasks/{run_id}/confirm`

DB design:

- `agent_runs` stores run-level state, task, plan, allowed tools, progress,
  report path, errors, total tool calls, latency, estimated cost, and timestamps.
- `tool_traces` stores one row per tool call with input/output summaries, raw
  JSON, status, latency, errors, token/cost estimates, and timestamps.
- Optional later table: `prompt_versions`.

Tool system:

- Tool Registry with `ToolSpec`, `ToolResult`, `register_tool`, `list_tools`,
  and `execute_tool`.
- Minimum tools: `file_reader`, `sql_query`, `rag_search`, `report_writer`.
- Later enhancement: read-only `mcp_github_search` via GitHub API/mock first;
  MCP integration is optional and should not block MVP.

Trace requirements:

- Every tool call must be persisted.
- Failed calls must also generate traces.
- Safety rejections must be visible through traces.
- Trace API is a core project feature, not just logging.

Evaluation requirements:

- At least 10 eval cases: file reading, SQL, RAG, mixed tasks, and safety/failure
  tasks.
- Minimum target: 7+ successful tasks, all failure cases visible, and all tool
  calls trace-complete.
- Preserve 2-3 bad cases for README and interview explanation.

## Global Constraints

- Standalone repo, not `agent-service-toolkit`.
- No company-specific names in code, docs, commit messages, examples, or demo
  data.
- No secrets, tokens, `.env`, private data, caches, local DB files, bulky
  generated reports, or model artifacts committed.
- Tools are read-only by default.
- File tools must enforce `workspace/docs` whitelist and path traversal
  protection.
- SQL tools must allow only `SELECT`/`WITH` and reject destructive statements.
- MCP/GitHub tools must be read-only and must not block MVP.
- Every tool call must create a trace record.
- Every phase must end with a checkpoint commit and GitHub push.

## Phase Plan: Day 1-5 / Day 6-9 / Day 10-14 / Day 15-19

### Phase 1: Day 1-5

- Read GPT Researcher and project planning document.
- Create/confirm `AGENTS.md` and `task.txt`.
- Bootstrap FastAPI skeleton.
- Implement `/health`.
- Add SQLite + SQLAlchemy with `agent_runs` and `tool_traces`.
- Implement minimal `POST /api/tasks` that creates a run record.
- Implement Tool Registry base abstractions.
- Implement `GET /api/tools`.
- Run smoke checks, update task log, commit, and push.

### Phase 2: Day 6-9

- Implement `file_reader` with whitelist, extension checks, and `max_chars`.
- Implement `sql_query` with read-only SQL enforcement and limits.
- Create demo SQLite data.
- Implement RAG loader/chunker/embedding/vector store.
- Register `rag_search`.
- Persist traces for success, failure, and safety rejection paths.
- Add smoke tests for file, SQL, and RAG.
- Update task log, commit, and push.

### Phase 3: Day 10-14

- Implement deterministic JSON Planner.
- Store `plan_json` in `agent_runs`.
- Implement Executor step loop that calls only registered tools.
- Implement Reporter to generate structured Markdown from task, plan,
  observations, and evidence.
- Implement full status, trace, and report endpoints.
- Handle missing tool, invalid args, illegal SQL, missing file, empty RAG, and
  model timeout without crashing service.
- Run end-to-end smoke tests, update task log, commit, and push.

### Phase 4: Day 15-19

- Implement `waiting_human` and confirmation endpoint.
- Implement read-only GitHub/MCP tool using API/mock first.
- Add Dockerfile and docker-compose.
- Add at least 10 eval cases and eval report generation.
- Complete README: startup, API, trace, DB, eval, bad cases, and interview
  explanation.
- Run Docker/eval checks, update task log, commit, and push.

## Current Phase Checklist

Current phase: Phase 1 - Day 1-5.

- [x] Confirm independent repository path.
- [x] Clone empty independent GitHub repository.
- [x] Read project constraints from root `AGENTS.md` and initial prompt.
- [x] Read project planning `.docx`.
- [x] Locate GPT Researcher source; clone read-only reference because no local
  source existed.
- [x] Read GPT Researcher key files and summarize conclusions.
- [x] Create repository `AGENTS.md`.
- [x] Create repository `task.txt`.
- [ ] Bootstrap FastAPI skeleton.
- [ ] Implement `/health`.
- [ ] Add SQLite + SQLAlchemy models.
- [ ] Implement minimal task creation.
- [ ] Implement Tool Registry base and `/api/tools`.
- [ ] Run Phase 1 smoke checks.
- [ ] Phase 1 checkpoint commit and push after skeleton work.

## Completed Work Log

2026-06-15:

- Cloned independent repository to `E:\BOSS\traceable-research-agent`.
- Confirmed repository was empty.
- Read root planning materials under `E:\BOSS`.
- Extracted the `.docx` planning document with Python `python-docx`.
- Cloned GPT Researcher into `E:\BOSS\_reference_gpt_researcher` as a read-only
  reference because no local GPT Researcher source existed.
- Read GPT Researcher source/design files listed above.
- Created repository `AGENTS.md` with project constraints, target architecture,
  required APIs, state machine, DB requirements, tool registry rules, phase
  plan, and checkpoint rules.
- Created repository `task.txt` with reading conclusions, phase plan, current
  checklist, command log, risks, and next actions template.
- Added `.gitignore` to prevent committing secrets, local DBs, caches, generated
  reports, and build artifacts.

## Commands Run

- `Get-ChildItem -Force`
- `Test-Path 'E:\BOSS\traceable-research-agent'`
- `git -C 'E:\BOSS\traceable-research-agent' status --short --branch`
- `git clone https://github.com/piao666/traceable-research-agent.git 'E:\BOSS\traceable-research-agent'`
- `Get-Content -Raw 'E:\BOSS\AGENTS.md'`
- `Get-Content -Raw 'E:\BOSS\task.txt'`
- `Get-Content -Raw 'E:\BOSS\CODEX_INITIAL_PROMPT.md'`
- Python `python-docx` extraction for `Traceable_Research_Agent_*.docx`
- `Get-ChildItem -Path 'E:\BOSS' -Recurse ...`
- `git clone --depth 1 https://github.com/assafelovic/gpt-researcher.git 'E:\BOSS\_reference_gpt_researcher'`
- `rg --files 'E:\BOSS\_reference_gpt_researcher' ...`
- `Get-Content` / `Select-String` reads for GPT Researcher files.

## Test / Smoke Results

- No application tests or smoke checks yet. This round intentionally avoided
  core feature implementation.
- Repository initialization and document/source reading completed.

## Git Checkpoints

Suggested checkpoint commit messages:

- `checkpoint(day1-5): bootstrap api database and tool registry`
- `checkpoint(day6-9): implement file sql rag tools with traces`
- `checkpoint(day10-14): complete planner executor reporter e2e loop`
- `checkpoint(day15-19): add hitl github docker eval and docs`

Actual checkpoints:

- 2026-06-15: initial documentation checkpoint pushed.
  - Commit: `dbea431` (`chore: initialize project planning docs`)
  - Files: `.gitignore`, `AGENTS.md`, `task.txt`
  - Remote: `origin/main`
  - Scope: constraints, reading notes, phase plan, task ledger, ignore rules.

## Open Issues / Risks

- The independent repo started empty, so Phase 1 implementation still needs the
  initial FastAPI/database/tool registry skeleton.
- A first normal `git clone` left a temporary Git lock due local permissions;
  it was cleaned before writing project files.
- GPT Researcher is large and should remain a reference only; avoid copying
  source structure blindly.
- MCP should remain late-stage and optional to avoid blocking the MVP.
- RAG backend choice is still open; prefer a lightweight local implementation
  unless Phase 2 needs Chroma/FAISS.

## Next Actions

Recommended next Phase 1 start:

1. Create minimal Python project structure and dependencies.
2. Add FastAPI app with `GET /health`.
3. Add SQLAlchemy database setup and `agent_runs` / `tool_traces` models.
4. Implement minimal `POST /api/tasks` to create a pending run row.
5. Implement Tool Registry base and `GET /api/tools`.
6. Run local smoke checks, update `task.txt`, then checkpoint commit and push.
